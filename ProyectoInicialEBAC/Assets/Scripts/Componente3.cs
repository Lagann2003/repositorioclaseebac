using UnityEngine;

public class Componente3 : MonoBehaviour
{
   void Start()
    {
        Componente1.MiObjeto.name = "Nuevo Nombre";
    }
    // Start is called once before the first execution of Update after the MonoBehaviour is created

    // Update is called once per frame
    void Update()
    {
        
    }
}
